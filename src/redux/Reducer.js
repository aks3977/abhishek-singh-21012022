
const initialState = {
    tasks: [],
    task: {},
    loading: false
}

const taskReducers = (state= initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default taskReducers;